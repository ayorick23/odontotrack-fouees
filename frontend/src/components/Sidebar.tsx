// Placeholder de la barra lateral de navegación.
// Cuando se defina el diseño final, aquí irán los links según el rol
// del usuario autenticado (admin, docente, estudiante, etc.).
export function Sidebar() {
  return (
    <aside>
      <p>Sidebar (pendiente de diseño)</p>
    </aside>
  );
}
