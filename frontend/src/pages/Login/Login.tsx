// Pantalla de inicio de sesión. Pendiente: formulario conectado a
// POST /api/auth/token/ (JWT) y guardar el token con useAuth.
export function Login() {
  return (
    <div>
      <h1>Login</h1>
    </div>
  );
}
